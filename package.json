{
  "name": "rollup-plugin-sass-variables-plus",
  "version": "0.0.1",
  "description": "load sass/scss variables as javascript object without camel case based on repo: https://www.npmjs.com/package/rollup-plugin-sass-variables",
  "main": "dist/rollup-plugin-sass-variables-plus.cjs.js",
  "module": "dist/rollup-plugin-sass-variables-plus.es.js",
  "repository": "https://github.com/Soni-L/rollup-plugin-sass-variables-plus",
  "scripts": {
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && rollup -c",
    "build:watch": "npm run clean && rollup -c -w",
    "prepublishOnly": "npm run build"
  },
  "author": "soni-lamkaj",
  "license": "MIT"
}
